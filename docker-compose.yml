
services:
  agy-bridge:
    build: .
    container_name: rocketchat-agy-bridge
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./workspace:/workspace
      - ./.gemini:/root/.gemini
      - ./skills:/root/.gemini/antigravity-cli/skills
    restart: unless-stopped
